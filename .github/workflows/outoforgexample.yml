name: Out of Org
on: [push, workflow_dispatch]

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # This is pinned to EXTERNAL/blog_gh_sha_pinning_action
      # Malicious commit, note action owner, this should fail
      - uses: aidenvaines-cgi/blog_gh_sha_pinning_action@6d7ec8077aaa08741e1b1ed6c014b3cdcafa894f
