name: In Org
on: [push, workflow_dispatch]

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # This is pinned to vainesorg/blog_gh_sha_pinning_action
      - uses: vainesorg/blog_gh_sha_pinning_action@7d6ad4affc79375ac049e2f4101d09f0fb5f4319 # 1.0.0
